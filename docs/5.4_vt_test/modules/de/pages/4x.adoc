= 4.x Versionshinweise
:revdate1: 2025-02-19
:page-revdate: {revdate}
:page-opendocs-origin: /14.releasenotes/03.4x/03.4x.md
:page-opendocs-slug:  /releasenotes/4x

== Versionshinweise für 4.x

=== 4.4.4-s3 Sicherheitspatch April 2022

* Aktualisieren Sie alle Bilder zu beheben hohe https://nvd.nist.gov/vuln/detail/CVE-2022-28391[CVE-2022-28391] in busybox (alpine).

=== 4.4.4-s2 Sicherheitspatch März 2022

* Update zur Behebung von CVE-2022-0778, einer OpenSSL-Schwachstelle, die im Alpine-Basis-Image gefunden wurde, das von {product-name} -Images verwendet wird. Kurze Beschreibung: Es ist möglich, eine Endlosschleife auszulösen, indem ein Zertifikat mit ungültigen elliptischen Kurvenparametern erstellt wird. Da das Parsen von Zertifikaten vor der Überprüfung der Zertifikatsignatur erfolgt, kann jeder Prozess, der ein extern bereitgestelltes Zertifikat parst, einem Denial-of-Service-Angriff ausgesetzt sein. Weitere Einzelheiten finden Sie unter den folgenden Links.
 ** https://security.alpinelinux.org/vuln/CVE-2022-0778
 ** https://www.suse.com/security/cve/CVE-2022-0778.html
 ** https://nvd.nist.gov/vuln/detail/CVE-2022-0778

=== 4.4.4-s1 Sicherheits-Patch Februar 2022

* Aktualisieren Sie Alpine in Manager, um aktuelle CVEs zu entfernen, einschließlich der hohen Bewertungen CVE-2022-25235, CVE-2022-25236 und CVE-2022-25314
* HINWEIS: Kürzlich wurden auch CVEs im Manager CLI-Modul veröffentlicht, die sich auf das Python-Paket beziehen. Das Python-Paket wird in der Version 5.0 durch Python3 ersetzt, um alle CVEs zu entfernen. Die Generalversammlung ist derzeit für Mai 2022 geplant. Die Befehlszeilenschnittstelle (CLI) ist nicht aus der Ferne zugänglich und kann nicht über die grafische Benutzeroberfläche (GUI) aufgerufen werden. Daher schützen geeignete Kubernetes-RBACs zur Einschränkung von "kubectl exec"-Befehlen im Manager-Pod vor Angriffen.
* Liste der Manager 4.4.4 CVEs
 ** alpin:3.15.0 Hoch CVE-2022-25235 https://cve.mitre.org/cgi-bin/cvename.cgi?name=CVE-2022-25235 expat
 ** alpine:3.15.0 Hoch CVE-2022-25236 https://cve.mitre.org/cgi-bin/cvename.cgi?name=CVE-2022-25236 expat
 ** alpine:3.15.0 Medium CVE-2022-25313 https://cve.mitre.org/cgi-bin/cvename.cgi?name=CVE-2022-25313 expat
 ** alpine:3.15.0 Hoch CVE-2022-25314 https://cve.mitre.org/cgi-bin/cvename.cgi?name=CVE-2022-25314 expat
 ** alpine:3.15.0 Hoch CVE-2022-25315 https://cve.mitre.org/cgi-bin/cvename.cgi?name=CVE-2022-25315 expat
 ** alpine:3.15.0 Medium CVE-2020-26137 https://cve.mitre.org/cgi-bin/cvename.cgi?name=CVE-2020-26137 usr/lib/python2.7/site-packages/urllib3-1.25.3
 ** alpine:3.15.0 Hoch CVE-2020-7212 https://github.com/advisories/GHSA-hmv2-79q8-fv6g usr/lib/python2.7/site-packages/urllib3-1.25.3
 ** alpine:3.15.0 Hoch CVE-2021-33503 https://github.com/advisories/GHSA-q2q7-5pp4-w6pg usr/lib/python2.7/site-packages/urllib3-1.25.3
 ** alpine:3.15.0 Medium CVE-2021-3572 https://github.com/advisories/GHSA-5xp3-jfq3-5q8x usr/lib/python2.7/site-packages/pip-20.3.4

=== Sonstige Aktualisierungen Februar 2022

* Aktualisieren Sie Helm Chart auf 1.9.1. Erlaubt Benutzern, verschiedene Bild-SHA-Hashes anstelle von Tags anzugeben, Unterstützung für k3s in der Rancher-Benutzeroberfläche hinzufügen.
* Community Operator wird auf 1.3.5 aktualisiert, um 4.4.4 zu unterstützen.
* Certified Operator wurde auf 1.3.2 aktualisiert, um 4.4.4 zu unterstützen.

=== 4.4.4 Februar 2022

==== Erweiterungen

* Hinzufügen einer Umgebungsvariable für Enforcer, um das Scannen von Geheimnissen zu deaktivieren, das in manchen Umgebungen Ressourcen verbrauchen kann. Eingestellt auf ENF_NO_SECRET_SCANS=1
* In Vulnerability Explorer > CSV-Download werden betroffene Container in mehreren Zeilen statt in einer Zelle angezeigt.

==== Fehlerbehebungen

* Verringern Sie das Scannen von Geheimnissen durch Enforcer, um zu vermeiden, dass lange laufende Scan-Aufgaben Speicherplatz verbrauchen können. Dies kann durch eine große Bildregistrierung oder einen lokalen Datenbankscan verursacht werden.
* Behebung eines Fehlers beim Versuch, CSV für CVEs zu exportieren, die im Schwachstellen-Explorer Security Risks -> Vulnerabilities gefunden wurden, ohne einen Filter zu verwenden, ist die CSV-Datei leer.
* Behebung eines Timing-Problems beim Upgrade von 4.2.2, das zu einer impliziten Verweigerung für den gesamten Datenverkehr führen kann. Die jüngste Korrektur bezieht sich auf die XFF-Einstellungen bei rollenden Updates.

==== Andere

* Erlaubt es Benutzern, verschiedene Bild-SHA-Hashes anstelle von Tags anzugeben https://github.com/neuvector/neuvector-helm/pull/140. Wird an Operator weitergegeben.

=== 4.4.3 Januar 2022

==== Erweiterungen

* Ersetzen Sie das selbstsignierte Zertifikat für Manager, das am 23. Januar 2022 abläuft, durch ein neues, das im Januar 2024 abläuft.
* Verbesserung der Möglichkeit, nicht verwaltete Arbeitslasten in der Netzwerkaktivitätskarte anzuzeigen, die nicht relevant sind.

==== Fehlerbehebungen

* Behebung von Controller-Abstürzen beim Scannen der Gitlab-Registrierung.
* Die Einlasskontrolle blockiert einige Bilder nicht. Dies liegt daran, dass eine Sicherheitslücke, die in mehreren Paketen gefunden wird, als 1 Sicherheitslücke in der Zulassungskontrolle von Controller behandelt und behoben wird.
* Ein Upgrade von 4.2.2 auf 4.3.2 führt zu einer impliziten Verweigerung des gesamten Datenverkehrs bei hohem Datenverkehr während des Rolling Upgrade.

==== Andere

* Helm Chart v1.8.9 wurde für 5.0.0-Einsätze veröffentlicht.

=== 4.4.2 Dezember 2021

==== Erweiterungen

* Hinzufügen von Unterstützung für das Scannen von eingebetteten Java-Jars und Jars ohne Maven-Datei, z.B. log4j-core-2.5.jar, wenn pom.xml nicht existiert.
* Hinzufügen der CVE-Datenbank-Quelle von https://github.com/advisories?query=maven[GitHub-Beratungen für Maven], beginnend mit scanner/CVE db Version 2.531.
* Die xref:automation.adoc#_rest_api[Referenzdokumentation] der Rest-API wurde auf 4.4.1 und 4.4.2 aktualisiert.

==== Fehlerbehebungen

* Behebung eines in Enforcer entdeckten Speicherlecks.

=== 4.4.1 Dezember 2021

==== Erweiterungen

* Hinzufügen von Unterstützung für cgroup v2, die für einige Umgebungen wie SUSE Linux Enterprise Server 15 SP3 erforderlich ist.

==== Fehlerbehebungen

* Behebung des Problems, dass Enforcer nicht in der Lage ist, CVE-2021-44228 in laufenden Containern zu erkennen.
* Reduzierung/Fixierung der hohen Speichernutzung durch Enforcer für einige Umgebungen.
* Behebung eines Problems beim Import/Export der Gruppenrichtlinie nv.ip.
* Problem beim Entfernen einer Gruppe ohne Container-Mitglieder behoben.
* Behebung des Problems, dass man sich mit neuvector-prometheus-exporter zeitweise nicht anmelden kann.
* Behebung des Problems, dass der REST-API-Endpunkt /v1/response/rule?scope=local nicht alle Antwortregeln löscht.

==== Helm Chart Update 1.8.7

* Unterstützung von Affinitäts- und Toleranzanpassungen für Controller, Scanner und Manager.
* Hinzufügen von nodeSelector-Unterstützung für Controller-, Manager-, Scanner- und Updater-Pods.
* Unterstützung von benutzerdefinierten Umgebungsvariablen für Controller-Container.

==== Splunk App veröffentlicht

* Neue Splunk-App für {product-name} ist veröffentlicht unter https://splunkbase.splunk.com/app/6205/

=== 4.4.0 Dezember 2021

==== Erweiterungen

* Hinzufügen der Möglichkeit, eine Schwachstelle (CVE) zu "akzeptieren", um sie von Berichten, Ansichten, Risikobewertungen usw. auszuschließen. Aus verschiedenen Bildschirmen wie Sicherheitsrisiken -> Schwachstellen, Assets -> Container usw. kann eine Schwachstelle ausgewählt und die Schaltfläche Akzeptieren angeklickt werden. Sobald es akzeptiert wurde, wird es der Liste der Sicherheitsrisiken -> Schwachstellenprofile hinzugefügt. Sie kann hier eingesehen, exportiert und bearbeitet werden. Beachten Sie, dass diese Akzeptanzfunktion auf aufgelistete Bilder und/oder Namensräume beschränkt werden kann. Neue Einträge können von diesem Bildschirm aus auch manuell zu dieser Liste hinzugefügt werden.
* Aktivieren Sie eine Konfigurationsbewertung einer Kubernetes-Bereitstellungs-yaml-Datei. Laden Sie eine yaml-Datei aus der Richtlinie -> Admission Control hoch, und sie wird mit allen Regeln der Admission Control abgeglichen, um zu sehen, ob sie auf eine Regel trifft. Ein Bericht über die Bewertung kann von diesem Fenster heruntergeladen werden.

==== Fehlerbehebungen

* Fixed Packet Capture ist nicht verfügbar für Pods mit istio Sidecar Proxy.
* Schreiben von Allinone nach /dev/null.json entfernen

=== 4.3.2-s1 November 2021

* Sicherheits-Patch-Veröffentlichung, die Schwachstellen in 'curl'-bezogenen Bibliotheken behebt, die in der Version 4.3.2 entdeckt wurden. Die entdeckten CVEs sind CVE-2021-22945, CVE-2021-22946 und CVE-2021-22947.

=== 4.3.2 September 2021

==== Erweiterungen

* Unterstützung von Openshift CIS Benchmark 1.0.0 und 1.1.0.
* Unterstützung der Option "Trockenlauf" der Einlasskontrolle.
* Verbesserung der Beschreibung der Quelle der Zulassungskontrollkriterien. Verbesserung der Kennzeichnungskriterien bei der Zulassungskontrolle, um weitere Kriterien hinzuzufügen.
* Unterstützung der Gitlab Cloud (SaaS) Registry-Suche.
* Unterstützt das Scannen von Bildern mit mehreren Architekturen.
* ConfigMap-Override-Option zum Zurücksetzen der Konfiguration beim Start des Controllers. Die Option 'always_reload: true' kann in jeder configMap yaml verwendet werden, um das Neuladen dieser yaml bei jedem Start des Controllers zu erzwingen.
* Vorgefertigte PSP-Best-Practice-Zugangskontrollregeln einbeziehen.
* Testen Sie die Unterstützung des AppArmor-Profils für die Ausführung von {product-name} als nicht-privilegierte Container.
* Benutzer können in der Liste der Sicherheitsereignisse auf den Gruppennamen klicken, um zur Auswahl der Richtlinie -> Gruppen zu gelangen.

==== Fehlerbehebungen

* Kennzeichen für Zulassungskontrollkriterium hinzufügen, um festzustellen, ob ein Scan-Ergebnis erforderlich ist.
* Warnung, wenn nicht alle Komponenten von {product-name} die gleiche Version verwenden.
* Docker Swarm/Mirantis-Plattform in Netzwerkaktivität anzeigen -> Ansicht -> System anzeigen. Dies wird durch Hinzufügen der Umgebungsvariablen für den Enforcer NV_SYSTEM_GROUPS aktiviert.

==== Andere

* Aktualisieren der Cronjob-Version in der Helmtabelle (v. 1.8.3).
* Unterstützung der Jenkins-Master-Slave-Konfiguration im Jenkins-Plugin.

=== 4.3.1 August 2021

==== Erweiterungen

* Knotenbeschriftungen unter Assets -> Knoten anzeigen.
* Anzeigen von Statistiken für den Controller in Assets -> Systemkomponenten
* Melden Sie, ob eine Sicherheitslücke in den Basisbildebenen bei der Bildsuche vorhanden ist, wenn Sie die REST-API zum Scannen von Bildern verwenden. Das Basisbild muss im Api-Aufruf angegeben werden, wie im folgenden Beispiel.

[,shell]
----
curl -k -H "Content-Type: application/json" -H "X-Auth-Token: $_TOKEN_" -d '{"request": {"registry": "https://registry.hub.docker.com/", "repository": "garricktam/debian", "tag": "latest", "scan_layers": false, "base_image": "2244...../nodejs:3.2......"}}' "https://$RESTURL/v1/scan/repository"
{noformat}
----

Beschränkungen:
Handelt es sich bei dem zu scannenden Bild um ein entferntes Bild mit der Angabe "registry", muss auch das Basisbild ein entferntes Bild sein und der Name muss mit http oder https beginnen.
Zum Beispiel,

[,shell]
----
{"request": {"repository": "neuvector/manager", "tag": "4.0.2", "scan_layers": true, "base_image": "alpine:3.12.0"}}
{"request": {"registry": "https://10.1.127.12:5000/", "repository": "neuvector/manager", "tag": "4.0.0", "scan_layers": true, "base_image": "https://registry.hub.docker.com/alpine:3.12.0"}}
{"request": {"repository": "neuvector/manager", "tag": "4.0.2", "scan_layers": true, "base_image": "10.1.127.12:5000/neuvector/manager:4.0.2”}}
----

==== Fehlerbehebungen

* Die Höhe der Vollstreckerliste ist einstellbar.
* Bereinigen Sie alle angezeigten Felder, um XSS-Angriffe zu verhindern.

=== 4.3 Juli 2021

==== Erweiterungen

* Die neue Anzeige der Netzwerkaktivitäten in der Konsole verbessert die Leistung und das Design der Objektsymbole. Das neue UI-Framework verbessert die Ladezeiten für Tausende von Objekten, die angezeigt werden sollen, drastisch. Sitzungsfilter werden bis zur Abmeldung in den Menüs Netzwerkaktivitäten, Sicherheitsrisiken und anderen Menüs beibehalten. Die GPU-Beschleunigung ist aktiviert, kann aber deaktiviert werden, wenn dies zu Anzeigeproblemen führt. Anmerkung: Bekanntes Problem mit bestimmten Windows-PCs mit aktivierter GPU.
* Hinzufügen der Möglichkeit, Gruppenrichtlinien (CRD-Datei im Yaml-Format) von der Konsole zu importieren, um Nicht-Kubernetes-Umgebungen zu unterstützen. Das ist wichtig: Importierte CRDs aus der Konsole werden NICHT als CRD-Regeln klassifiziert und angezeigt. Sie können über die Konsole bearbeitet werden, im Gegensatz zu CRDs, die über Kubernetes angewendet werden.
* Unterstützung mehrerer Web-Hook-Endpunkte. Unter Einstellungen -> Konfiguration können mehrere Web-Hook-Endpunkte hinzugefügt werden. In Antwortregeln kann der Benutzer beim Erstellen einer Regel auswählen, welche(r) Endpunkt(e) per Webhook benachrichtigt werden sollen.
* Unterstützung der Konfiguration (mehrere Web-Hooks) in föderierten Regeln.
* Unterstützung des JSON-Formats für Web-Hooks. Bei der Erstellung eines Web-Hooks können jetzt JSON, Schlüssel-Wert-Paare oder Slack als Web-Hook-Formate konfiguriert werden.
* Unterstützung benutzerdefinierter Benutzerrollen für die Zuordnung zu einem Namespace-Benutzer. Die Verzeichnisintegration unterstützt die Zuordnung von Gruppen zu Rollen, wobei die Rolle auf den/die Namensraum/Namensräume beschränkt werden kann. Einschränkung: Wenn der Benutzer mehreren Gruppen angehört, wird die Rolle der ersten passenden Gruppe zugewiesen. Bitte beachten Sie die Reihenfolge der Konfiguration für das richtige Verhalten.
* Liste der externen IPs für Egress-Verbindungen herunterladen. Es wurde die Möglichkeit hinzugefügt, Berichte/CSV von der Dashboard-Seite unter dem Abschnitt Ingress und Egress Exposure herunterzuladen.
* Unterstützung von cve-medium Kriterien in Antwortregeln.
* Hinzufügen einer vorkonfigurierten PSP-Best-Practice-Regel zu den Regeln der Zulassungskontrolle. Zum Beispiel können die folgenden voreingestellten Kriterien einen Einsatz alarmieren/blockieren: Als Privilegierter ausführen, Als Root ausführen, IPC-Namensräume des Hosts freigeben = wahr, Netzwerk des Hosts freigeben = wahr, PIC-Namensräume des Hosts freigeben = wahr.
* Unterstützung der Verwendung von Namensräumen im erweiterten Filter für den Bericht "Sicherheitsrisiken, Schwachstellen und Compliance für Assets" im PDF-Format.
* Unterstützung von Kriterien für Zulassungskontrollregeln auf der Grundlage von CVE-Scores.
* Hinzufügen einer Schaltfläche "Test Registry" bei der Konfiguration von Registry-Scans für Registries, die diese Funktion unterstützen, wie Docker und JFrog.
* Verbessertes Herunterladen von Support-Protokollen und Debug-Einstellungen für Controller. Aktivieren Sie die Download-Einstellungen, wie z. B. den cPfad und welche Komponentenprotokolle heruntergeladen werden.
* Unterstützung für Kubernetes 1.21 hinzugefügt.

==== Fehlerbehebungen

* Unterstützung von Kubernetes 1.21 mit containerd 1.4.4. Die Containerd-Laufzeit v1.4.4 ändert ihre cgroup-Darstellungen.
* Scanner identifiziert OS als ol:7.9 mit falsch positiven CVEs.
* Unterstützung der eigenständigen Scanner-Bereitstellung auf der Azure DevOps-Erweiterung.

==== Andere Änderungen

* Helm Chart v1.8.0 wurde aktualisiert, um registry.neuvector.com als Standardregistrierung zu verwenden. HINWEIS: Sie müssen das Versionskennzeichen manuell angeben.
* Hinzufügen konfigurierbarer Parameter wie Controller-API-Anmerkungen im Helm-Diagramm. Verfügbar ab Version 1.7.6+.
* Community Operator 1.2.6, Certified Operator 1.2.7 aktualisiert, um Helm-Diagrammaktualisierungen widerzuspiegeln, einschließlich des Hinzufügens einer OpenShift-Route, wenn controller.apisvc.type aktiviert ist. Der zertifizierte Operator 1.2.7 setzt {product-name} Version 4.2.2 ein.
* Hinzufügen eines HTML-Ausgabeformats für Scanergebnisse in Jenkins-Pipeline-Scanergebnissen.
* Hinzufügen des Namespace des betroffenen Workloads in Prometheus-Exporter-Warnungen. Jetzt unterstützt in neuvector/prometheus-exporter:4.2.2 und später.

=== 4.2.2 April 2021

==== Erweiterungen

* Aktivieren Sie die Durchsetzung einer Kennwortrichtlinie. Wenn diese Funktion aktiviert ist, müssen die Kennwörter den konfigurierten Mindestsicherheitsanforderungen entsprechen. Gehen Sie zu Einstellungen - Benutzer/Rollen, um die Kennwortrichtlinie festzulegen, einschließlich der erforderlichen Mindestanzahl von Zeichen, Großbuchstaben, Zahlen und Sonderzeichen. Auch Verwechslungen und die Wiederverwendung von Passwörtern werden verhindert.
* Schrägstrich in Schlüssel/Wert in CRD-Gruppendefinition zulassen.
* Erweiterung von SAML zur Unterstützung der CAC-Authentifizierung. SAML AFDS Common Access Card (CAC) Authentifizierungsmethode.
* Überprüfung der Kompatibilität mit OpenShift 4.7

==== Fehlerbehebungen

* Behebung des Problems, dass Enforcer den Neustart des Knotens beim OpenShift-Update um bis zu 20 Minuten verzögert.
* Korrigieren Sie die Terminologie für nicht verwaltete Knoten auf "Knoten".
* Der CRD-Import führte zu unerwarteten Ergebnissen. Unter {product-name} ist ein Konvertierungstool erhältlich, das die Konvertierung von früheren Versionen des CRD-Formats erleichtert.
* In AKS werden Webhook-Zertifikate ohne SAN für k8s v1.19+ erstellt.
* Die föderale Politik funktioniert uneinheitlich und nicht wie erwartet. Verbesserung der ip-Logik für nicht verwaltete Arbeitslasten, um unnötige Verstöße zu reduzieren.

=== 4.2.1 März 2021

==== Fehlerbehebungen

* Vordefinierte Dateizugriffsregeln werden in der Konsole nicht angezeigt.
* Die Spaltenüberschriften sind in mehreren Konsolenansichten falsch, z. B. in Assets->Registrierung->Modul-Suchergebnisse. Einige PDF-Berichte waren ebenfalls betroffen und wurden inzwischen korrigiert. Andere Bereiche, vor allem im Sonatype-Build, wurden behoben.

=== 4.2 März 2021

==== Erweiterungen

* Multicluster-Überwachung. Zentralisierte Sichtbarkeit der Sicherheitslage aller verwalteten Cluster durch Anzeige der Risikobewertung und Cluster-Zusammenfassung für jeden Cluster auf der Multi-Cluster-Verwaltungsseite. Hinweis: Für den Multicluster-Verbund ist eine separate Lizenz erforderlich.
* Hinzufügen von Unterstützung für die in IBM Cloud integrierte nutzungsbasierte Abrechnung.
* Erweiterung des PCI-Konformitätsberichts um eine Asset-Ansicht, in der die Schwachstellen nach Dienst aufgelistet sind.
* Fügen Sie eine Zusammenfassung der Scanergebnisse hinzu, bevor Sie die Schwachstelle auflisten.
* Unterstützung der Red Hat OVAL2-Datenbank, die für die Zertifizierung von Red Hat Vulnerability Scanner erforderlich ist.
* Unterstützung der Red Hat OpenShift Beta-Version der CIS-Benchmarks ("inspired by CIS"). Die endgültige Fassung wird bei der Veröffentlichung der offiziellen Version durch CIS.org vorliegen. Diese Funktion wird für Bereitstellungen von OpenShift Version 4.3+ unterstützt.
* Erlauben Sie die Filterung von API-Abfragen, um Bedingungen wie erlaubte oder verweigerte Bilder bei API-Aufrufen zu prüfen.
* Hinzufügen von Unterstützung für CIS Kubernetes Benchmark 1.6.0.
* Meldung und Anzeige der während des Scans gefundenen Bildmodule in den Scanergebnissen. Dies wird auf einer Registerkarte in den Bildsuchergebnissen angezeigt und ist in den Suchergebnissen der REST-API enthalten.
* Ermöglicht die Bearbeitung von Filtern in Registrierungs-, Gruppen- und Antwortregelkonfigurationen über die Konsole.
* ConfigMap aktualisieren, um group_claim in oidcinitcfg.yaml und samlinitcfg.yaml und Xff_Enabled in sysinitcfg.yaml hinzuzufügen
* Die yaml der API wurde für 4.2 im xref:automation.adoc#_rest_api[Abschnitt Automatisierung] aktualisiert.

==== Fehlerbehebungen

* Der Enforcer ist nicht in der Lage, einem bestehenden Cluster beizutreten, was in Fällen, in denen zu viele Enforcer registriert sind, manchmal 10 Minuten dauert. Dies ist der Fall, wenn Enforcer ungewollt beendet werden, aber immer noch für Lizenzprüfungen registriert sind, wodurch andere Enforcer daran gehindert werden, sich anzuschließen, wenn die Lizenzgrenze erreicht ist.
* Behoben: Wildcard-DNS-Verkehr blockiert. Die Zwischenspeicherung von DNS-Ergebnissen, die mit einer Wildcard-DNS-Adressgruppe übereinstimmen, wurde verbessert.
* Behebung der seltenen Situation, dass CRD-Zertifikate für Webhook und Controller nicht synchronisiert sind.
* Korrigieren Sie die Legende in der Netzwerkaktivitätsanzeige für "Unmanaged" auf "Nodes".
* Als Arbeitsbelastung erkannte Knoten, die zu impliziten Verstößen führen.

==== Andere

* Jenkins Plugin Erweiterungen:
 ** Überschreiben Sie den Schweregrad der Schwachstelle nach Punkten.
 ** Hinzufügen von Fehlermeldungen zu den JAVA-Ausnahmen hudson.AbortException.
* Aktualisieren Sie die Helm-Karte auf 1.7.1.

Einzelheiten finden Sie in den Versionshinweisen im Abschnitt xref:other.adoc[Integrationen und andere Komponenten].

=== 4.1.2 Februar 2021

==== Erweiterungen

* Aktivieren Sie die Umschaltung für XFF-Weiterleitung, um die standardmäßig aktivierte {product-name} -Richtlinie zu deaktivieren. Dies hängt mit einer Funktion zusammen, die in 4.1.1 hinzugefügt wurde, um Unterstützung für x-forwarded-*-Header hinzuzufügen. Um diese Funktion zu deaktivieren, gehen Sie zu Einstellungen -> Konfiguration. WICHTIG! Siehe die detaillierte Beschreibung des Verhaltens von XFF-FORWARDED-FOR weiter unten.

==== Fehlerbehebungen

* Es wurde behoben, dass CVE-2020-1938 nicht erkannt wird.
* Behebung des Fehlers vom Manager "Failed to export configurations of section {policy, user, config}."
* Der Filter Network Activity Graph funktioniert nicht.
* Verbessern Sie den CPU- und Speicherverbrauch des Controllers.

==== Andere

* Das Jenkins-Plugin wurde aktualisiert, um einen eigenständigen Scanner zu unterstützen.  Einzelheiten finden Sie in den Versionshinweisen im Abschnitt xref:other.adoc[Integrationen und andere Komponenten].

==== XFF-FORWARDED-FOR Verhaltensdetails

In einem Kubernetes-Cluster kann eine Anwendung über einen NodePort, einen LoadBalancer oder einen Ingress-Dienst nach außen hin zugänglich gemacht werden. Diese Dienste ersetzen in der Regel die Quell-IP, während sie das Source NAT (SNAT) an den Paketen vornehmen. Da die ursprüngliche Quell-IP maskiert ist, kann {product-name} nicht erkennen, dass die Verbindung tatsächlich von "extern" stammt.

Um die ursprüngliche Quell-IP-Adresse beizubehalten, muss der Benutzer die folgende Zeile zu den exponierten Diensten im Abschnitt "spec" des nach außen gerichteten Load Balancer oder Ingress Controllers hinzufügen. (Ref: https://kubernetes.io/docs/tutorials/services/source-ip/)

"externalTrafficPolicy":"Local"

Viele Implementierungen von LoadBalancer-Diensten und Ingress-Controllern fügen dem HTTP-Request-Header die Zeile X-FORWARDED-FOR hinzu, um den Backend-Anwendungen die tatsächliche Quell-IP mitzuteilen. In Version 4.1.0 haben wir eine Funktion hinzugefügt, die diese HTTP-Header erkennt, die ursprüngliche Quell-IP identifiziert und die Richtlinie entsprechend durchsetzt.

Diese Verbesserung führte zu einigen unerwarteten Problemen bei einigen Einstellungen. Wenn die obige Zeile zu den exponierten Diensten hinzugefügt wurde und die Netzwerkrichtlinien {product-name} so erstellt wurden, dass erwartet wird, dass die Netzwerkverbindungen von internen Proxy-/Eingangsdiensten kommen, weil wir jetzt erkennen, dass die Verbindungen von "extern" zum Cluster kommen, könnte normaler Anwendungsverkehr Warnungen auslösen oder blockiert werden, wenn die Anwendungen in den "Schutz"-Modus versetzt werden.

In 4.1.2 wurde ein Schalter hinzugefügt, um diese Funktion zu deaktivieren. Durch die Deaktivierung wird {product-name} angewiesen, die Verbindung nicht über X-FORWARDED-FOR-Header als "extern" zu identifizieren. Standardmäßig ist dies aktiviert, und der X-FORWARDED-FOR-Header wird bei der Durchsetzung von Richtlinien verwendet. Um sie zu deaktivieren, gehen Sie zu Einstellungen -> Konfiguration, und deaktivieren Sie die Einstellung "X-Forwarded-For based policy match".

image:xff.png[xff_behavior]

=== 4.1.1 Januar 2021

==== Fehlerbehebungen

* Hinzufügen von Unterstützung für AWS EKS AMI Release v20210112 zur Behebung von Ulimit-Problemen.

=== 4.1 Dezember 2020

==== Erweiterungen

* Benutzer können den Richtlinienmodus beim Export von CRD ändern.
* OIDC-Unterstützungsansprüche vom Endpunkt /oauth/userinfo.
* Unterstützung für die Aktualisierung von Clusterknoten, um das Wachstum von Knoten und die Migration von Pods zwischen Knoten vorübergehend zu unterstützen.
* Erstellen Sie einen Nutzungsbericht zum Herunterladen auf der Seite Einstellungen -> Konfiguration.
* Wildcard-Unterstützung für Namespace bei der Zuweisung von Benutzerrollen zu Namespace.
* Verbesserung der Logik zum Entfernen von Gruppen/Policen. Konfigurierbare Einstellung, wann eine unbenutzte Gruppe entfernt wird, basierend auf der Zeit, die seit ihrer letzten Verwendung vergangen ist.
* Erlaubt dem Benutzer, die Dauer der Paketaufnahme zu konfigurieren.
* Hinzufügen von Unterstützung für die Leserrolle "Multicluster-Management".
* Der eigenständige Scanner überträgt jetzt die Scanergebnisse über die REST-API. Siehe unten für Details zum Scanner.
* Erkennen und blockieren Sie den in CVE-2020-8554 gemeldeten Man-in-the-middle-Angriff.
* Hinzufügen von Unterstützung für gebührenpflichtige (nutzungsbasierte) Lizenzierungsmodelle.
* Entfernen Sie den Schritt für die Erstellung von CRDs (z.B. NvSecurityRule) aus den Beispiel-Deployment-Yamls für Kubernetes und Openshift. Dies ist nicht erforderlich (Controller erstellt diese automatisch). Der Einsatz des Steuermanns wird sich auch darum kümmern.

==== Fehlerbehebungen

* Verbesserung der hohen Speichernutzung auf Controller und Enforcer.
* Beim Versuch, einen Registrierungsfilter zu konfigurieren, wurde ein Fehler zurückgegeben. Erlaubt die Verwendung von Platzhaltern an beliebiger Stelle im Repo/Tag-Filter.
* Die Blockierungspolitik funktioniert nicht wie erwartet. Unterstützung für x-forwarded-*-Header hinzugefügt. WICHTIG! Siehe die ausführliche Beschreibung des Verhaltens von XFF-FORWARDED-FOR oben als Teil der 4.1.2 Release Notes.
* Helm Chart-Fehler bei der Einstellung von Controller-Ingress auf true.
* Aufgrund einer Gateway-Zeitüberschreitung kann keine Netzwerkregel hinzugefügt und gespeichert werden.
* In den Configmap-Beispielen fehlt das Feld Group_Claim. Zur xref:configmap.adoc[Configmap-Dokumentation] hinzugefügt.
* Prozessprofilverletzung beim Beenden des Controller-Pods.

==== Scanner-Details

Zwei zusätzliche Umgebungsvariablen werden hinzugefügt, um sich bei der Controller-REST-API anzumelden. Benutzer mit der CICD-Integrationsrolle können die Ergebnisse übermitteln.

Neue Umgebungsvariablen: SCANNER_CTRL_API_USERNAME, SCANNER_CTRL_API_PASSWORD

Beispiel für die Verwendung

[,shell]
----
docker run --name neuvector.scanner --rm -e SCANNER_REPOSITORY=ubuntu -e SCANNER_TAG=16.04 -e SCANNER_LICENSE=$license -e CLUSTER_JOIN_ADDR=10.1.2.3 CLUSTER_JOIN_PORT=32368 -e SCANNER_CTRL_API_USERNAME=username -e SCANNER_CTRL_API_PASSWORD=secret -v /var/run/docker.sock:/var/run/docker.sock -v /var/neuvector:/var/neuvector neuvector/scanner
----

==== Kubernetes 1.19+ und CRD-Exporte

[IMPORTANT]
====
Um eine exportierte CRD mit Kubernetes 1.19+ zu verwenden, entfernen Sie bitte die "Version: v1" aus jedem Abschnitt. Diese befindet sich am Ende oder in der Nähe des Endes eines jeden Abschnitts in einer exportierten Gruppenrichtlinien-CRD.
====

[,yaml]
----
    version: v1
----

=== 4.0.3 Dezember 2020

==== Fehlerbehebungen

* Beim Beenden des Controller-Pods ist eine Prozessprofilverletzung aufgetreten.
* Implizite Verstöße für vom Benutzer erstellte Adressgruppen, die Platzhalter in Hostnamen verwenden.

==== Änderungen der Steuerkarte

* Erlaubt dem Benutzer, die Prioritätsklasse des Managers/Controllers/Enforcers/Scanners anzupassen. Wir empfehlen, {product-name} Containern eine höhere Priorität einzuräumen, um sicherzustellen, dass die Sicherheitsrichtlinien durchgesetzt werden, wenn die Knotenressourcen unter Druck stehen oder während eines Cluster-Upgrade-Prozesses.
* Erstellen Sie eine separate Tabelle für CRD. Auf diese Weise können CRD-Richtlinien erstellt werden, bevor die {product-name} Kerndienste bereitgestellt werden. Wenn das neue Diagramm verwendet wird, sollten die CRD-Ressourcen im Kerndiagramm, die aus Gründen der Abwärtskompatibilität beibehalten werden, mit crdwebhook.enabled=false deaktiviert werden.
* Erlauben Sie dem Benutzer, das Dienstkonto für die Bereitstellung von {product-name} anzugeben. Zuvor wird das "Standard"-Dienstkonto des Namensraums verwendet. Wenn {product-name} zusammen mit anderen Anwendungen in einem Namensraum bereitgestellt wird, ist es für einige Benutzer nicht ratsam, das Standarddienstkonto für den Namensraum zu verwenden.

=== 4.0.2 Dezember 2020

==== Erweiterungen

* Konsole - die Container-Listenseite Assets -> Container sollten die Möglichkeit bieten, die Fenstertrenner zu ziehen, um die Größe zu ändern.
* Hinzufügen von Zugangskontrollprüfungen für Pod-Share-Host-Namespaces. Der Benutzer kann wählen, ob er verhindern will, dass der Pod die Namensräume Netzwerk, IPC und PID des Hosts gemeinsam nutzt. Siehe unten für weitere Einzelheiten.
* Möglichkeit, eine Liste von Containern zu exportieren, die als privilegiert oder als "runasroot" laufen.
* Aktivieren Sie unter Benachrichtigungen -> Sicherheitsereignisse die Anzeige von Informationen über die Ereignisattribute, ohne den Bildschirm wechseln zu müssen.

==== Fehlerbehebungen

* Problem mit Jumbo-Frames (bei einigen öffentlichen Clouds aktiviert). Symptom: Der Haupt-URI der prometheus-Anwendung /graph wird unzugänglich, wenn die prometheus-Gruppe in den Schutzmodus versetzt wird.
* Fehlende Namespace-Option im Verwundbarkeitsfilter. Erlauben Sie den Benutzern, den Namensraum, in dem {product-name} installiert ist, als Filtereintrag auszuwählen/anzugeben.
* Falsches Positiv in OpenSSL Version 1.1.1c-1 betroffen von CVE-2020-1967.
* Unerwartete implizite Ablehnungsverletzungen für eine vom Benutzer erstellte Adressgruppe mit Platzhalter-Hostnamen. Probleme bei der Verwendung von DNS-Namen (mit Platzhaltern) für Firewall-Datenverkehr.
* Verbesserung der Erkennung zur Beseitigung von SQL Injection False Positives.

==== Zulassungskontrolle für Pod-Sharing

. HostPID - Steuert, ob die Pod-Container den Namespace der Host-Prozess-ID gemeinsam nutzen können. Beachten Sie, dass dies in Verbindung mit ptrace dazu verwendet werden kann, die Privilegien außerhalb des Containers zu erweitern (ptrace ist standardmäßig verboten).
. HostIPC - Steuert, ob die Pod-Container den Host-IPC-Namespace gemeinsam nutzen können.
. HostNetwork - Steuert, ob der Pod den Node Network Namespace verwenden darf. Dadurch erhält der Pod Zugriff auf das Loopback-Gerät, auf Dienste, die auf localhost lauschen, und kann dazu verwendet werden, die Netzwerkaktivitäten anderer Pods auf demselben Knoten auszuspähen.

=== 4.0.1 November 2020

[IMPORTANT]
====
Änderungen in der Struktur der Steuerkarte

Das Verzeichnis für das Diagramm {product-name} hat sich von ./neuvector-helm/ zu ./neuvector-helm/charts/core/ geändert.

Wenn Sie Helm für das Upgrade verwenden, aktualisieren Sie bitte den Speicherort auf den oben angegebenen Pfad.
====

==== Erweiterungen

* Hinzufügen von Unterstützung für stromlinienförmiges Scannen von Bildern.
* Hinzufügen der Möglichkeit, einen Einzelbild-Scan aus der Registrierung auszulösen, wobei die Ergebnisse für die Zulassungskontrolle zur Verfügung stehen.
* Aktualisierung der JFrog Xray-Integration auf die neuen JFrog-Plattform-Api / Authentifizierungsanforderungen.
* Fügen Sie Informationen über Scanner im Manager hinzu, wie z. B. die Version und die Scanner-Statistiken.
* Schnellfilter hinzufügen, um Sicherheitsereignisse auszuschließen (ähnlich wie grep -v).
* Aktualisieren Sie den CVE-Schweregrad, um ihn mit den NVD-Schweregraden abzugleichen. Bei Verwendung des größeren der CVSS v2 und v3 Scores sind die Bewertungen Hoch für >=7, Mittel für >=4.
* Unterstützung von Standalone-Scannern für das lokale Scannen von Bildern (kein Controller erforderlich). Fügt neue Umgebungsvariablen SCANNER_LICENSE, SCANNER_REGISTRY, SCANNER_REPOSITORY, SCANNER_TAG, SCANNER_REGISTRY_USERNAME, SCANNER_REGISTRY_PASSWORD, SCANNER_SCAN_LAYERS, CLUSTER_JOIN_ADDR, CLUSTER_JOIN_PORT hinzu.
* Unterstützung der Auto-Vervollständigung von Namensräumen bei der Erstellung von Namensraum-Benutzern in Einstellungen -> Benutzer.
* Hinzufügen der Möglichkeit, ausgenommene CVEs in das Jenkins-Scanner-Plugin einzugeben.
* Hinzufügen von Zulassungskontrollkriterien, um Images blockieren zu können, bei denen der Scan das Betriebssystem nicht erkannt hat (z. B. Archlinux-Images) und daher keine Sicherheitslücken gefunden wurden. Ein neues Kriterium "Bild ohne OS-Informationen" wurde hinzugefügt. Wenn es auf "true" gesetzt ist, bedeutet dies, dass das Basis-Betriebssystem des Bildes nicht verfügbar ist.

==== Fehlerbehebungen

* Verbessern (verringern) Sie den Speicherverbrauch des Controllers.
* Aktivieren Sie die Unterstützung für Webhook-Funktionen wie Zulassungskontrolle und CRD in Kubernetes 1.19.
* Hinzufügen von Unterstützung für apiextensions.k8s.io/v1-Bereitstellungen, wie für Kubernetes 1.19 erforderlich (und in k8s 1.18 unterstützt).
* Unerwartete Verletzung der Prozessprofilregel durch ein übergeordnetes Shell-Skript für einen Prozess auf der Liste der erlaubten Prozesse.
* Hinzufügen von Unterstützung für Wildcard-Filter in der Harbor-Registrierung (konfiguriert mit der Docker-Registrierungseinstellung).
* Verbesserte Handhabung der Configmap zum Neuladen, wenn das Admin-Passwort auf den Standardwert zurückgesetzt wird. Dies soll einen unsicheren Zugriff verhindern, wenn das System nach einem Speicherausfall auf Clusterebene wiederhergestellt wird.

=== 4.0.0.s1 Oktober 2020

==== Sicherheits-Patch für {product-name} Containers

* Diese Sicherheitsfreigabe gilt für die Container {product-name} Manager und Allinone und adressiert die hohe https://cve.mitre.org/cgi-bin/cvename.cgi?name=CVE-2020-14363[CVE-2020-14363], die in der Alpine-Basisschicht im Paket libx11 gefunden wurde.  Als Teil des Updates wird auch das Medium CVE-2020-8927 behandelt. Obwohl es unwahrscheinlich ist, dass dieses Problem ausgenutzt werden kann, betrifft es die Manager-Konsole für {product-name} und hat keine Auswirkungen auf den Betrieb der Controller- oder Enforcer-Container.

=== 4.0 September 2020

==== Erweiterungen

* Anpassbare Compliance-Vorlagen. Voreingestellte Vorlagen für PCI, GDPR, HIPAA, NIST. Jeder CIS-Benchmark und jede benutzerdefinierte Prüfung kann mit einer oder mehreren Compliance-Vorschriften versehen werden. Anschließend können Berichte für jeden einzelnen erstellt werden. Sicherheitsrisiken -> Konformitätsprofil.
* Unterstützung des Vulnerability Management Workflows. Verfolgen Sie den Status von Sicherheitslücken und erstellen Sie Richtlinien auf der Grundlage des Datums der Entdeckung von Sicherheitslücken und anderer Kriterien. Sicherheitsrisiken -> Schwachstellen (erweiterter Filter) und Zugangskontrollregeln.
* Prüfung von Geheimnissen. Mehr als 20 Geheimhaltungsprüfungen sind enthalten und werden automatisch bei Image-Scans und Ressourcenabfragen durchgeführt. Die Ergebnisse werden in den Berichten über die Einhaltung der Vorschriften zu den Schwachstellen von Bildern in den Registern Assets -> und Security Risks -> Compliance als bestanden/gewarnt angezeigt.
* Granulare RBAC für {product-name} Benutzer. Erstellen Sie benutzerdefinierte Rollen mit granularen Lese-/Schreibberechtigungen für {product-name} Funktionen. Weisen Sie Benutzern Rollen zu. Einstellungen -> Benutzer/Rollen.
* Skalierbare und getrennte Scanner-Pods. Die Scanner-Pods können nach oben oder unten skaliert werden, um Tausende von Bildern zu scannen. Die Steuerung weist jedem verfügbaren Scanner-Pod Scan-Aufgaben zu. Wichtig: Der Controller enthält keine Scannerfunktion mehr, so dass mindestens ein Scanner-Pod benötigt wird, um eingesetzt zu werden. Außerdem sind die 4.x-Scanner NICHT abwärtskompatibel mit 3.x-Controllern. 3.x-Einsätze von externen Scannern sollten auf neuvector/scanner:3 aktualisiert werden.
* Serverloses Scannen und Risikobewertung für AWS Lambda. Scannen Sie AWS Lambda-Funktionen auf Schwachstellen mit dem Serverless IDE Plug-in oder in AWS-Konten. Zu den unterstützten Sprachen gehören Java, Python, Ruby und node.js. Führen Sie eine Risikobewertung durch, indem Sie die IAM-Rollenberechtigungen für Lambda-Funktionen auswerten, und schlagen Sie Alarm, wenn unnötige Berechtigungen aktiviert sind. Anmerkung: Serverlose Sicherheit erfordert eine separate {product-name} Lizenz.
* Führen Sie während des Scannens von Bildern Konformitätsprüfungen durch. Auch Bereitstellung yamls-Datei. Dazu gehören setuid, setgid, CIS (als root usw.) und 20+ Geheimnisse.
* Verbessern Sie den Sicherheitsrisiko-Score im Dashboard mit der Möglichkeit, zu aktivieren/deaktivieren, welche Gruppen zum Risiko-Score beitragen. Richtlinie -> Gruppen -> Kontrollkästchen Scorable. Dazu gehört auch die Möglichkeit, Systemcontainer von der Risikobewertung auszuschließen.
* Unterstützung für einen Benutzer mit eingeschränktem Namensraum, der Zugang zu zugewiesenen Registern hat.
* Verteilen Sie Scan-Syslog-Benachrichtigungen auf einzelne CVE-Syslog-Ereignisse.
* Erlauben Sie es einem auf einen Namensraum beschränkten Benutzer, Registrierungen zu erstellen, die nur für Benutzer mit Zugriff auf diesen Namensraum sichtbar sind (einschließlich globaler Benutzer).
* Laden Sie pdf-Berichte aus dem Dashboard nach Namensraum herunter. Wählen Sie einen Namensraum, um den Dashboard-PDF-Bericht zu filtern.
* Das CRD-Importverhalten wurde dahingehend geändert, dass der PolicyMode einer "verknüpften" Gruppe ignoriert wird und der PolicyMode unverändert bleibt, wenn die verknüpfte Gruppe bereits existiert. Wenn die verknüpfte Gruppe nicht existiert, wird sie automatisch erstellt und unter Einstellungen -> Konfiguration auf den Standardmodus Neue Dienste eingestellt. Eine "verknüpfte" Gruppe ist eine Gruppe, die nicht für den Export ausgewählt wurde, auf die aber durch eine Netzwerkregel verwiesen wird und die daher zusammen mit der/den ausgewählten Gruppe(n) exportiert wurde.

==== Fehlerbehebungen

* Die URL-Validierung in der Registrierung erlaubt URL ohne Protokollschema-Präfix. Validierung des Protokollschemas hinzugefügt.
* Container-Scans fehlgeschlagen - In manchen Situationen konnten Dateien nicht gelesen werden. Behebt den Fehler "Failed to read file - error=<nil>".
* Die Spalte Gruppenmitglied ist für die spezielle Gruppe "Knoten" ungenau.
* Abzug (Reduzierung) der Zulassungskontrollen (4 Punkte) von der Gesamtrisikobewertung für die Docker EE-Plattform, da sie nicht anwendbar sind.
* Bei einem reinen Scanner-Steuergerät kann es 15-20 Minuten dauern, bis es einsatzbereit ist.
* Sicherheitsrisiken > Schwachstellen "Schwere" Der Titel der Verteilung ist fälschlicherweise als Dringlichkeit bezeichnet.
* Sicherheitsereignisse Quelle Workload:Ingress-Regel stimmt nicht überein. Unerwartete implizite Verletzung von Workload:Ingress auf der OpenShift 3.11 Plattform. Die interne Subnetzlogik wurde verbessert, um große IP-Bereiche zu handhaben.
* Enforcer meldet einen Fehler beim Versuch, eine Verbindung zu /var/run/docker.sock herzustellen. Wiederherstellung bei Verbindungsabbruch hinzufügen.

==== Zusammenfassung der wichtigsten operationellen Änderungen

* Der 4.x Scanner ist NICHT kompatibel mit den 3.2.0, 3.2.1, 3.2.2 Controllern. Wenn Sie externe Scanner der Version 3.x eingesetzt haben und diese weiterhin laufen sollen, stellen Sie sicher, dass Sie den Scannereinsatz mit einem Tag der Version 3 versehen, z. B. neuvector/scanner:3. Alternativ können Sie auch auf 3.2.3+ aktualisieren.
* Lizenz zur Aktivierung der serverlosen Sicherheit erforderlich
* Neues Clusterolebinding und Clusterrole für Kubernetes und OpenShift hinzugefügt
* Der Controller hat keinen eingebauten Scanner mehr. Sie müssen mindestens 1 Scanner-Pod einsetzen.
* Änderungen der Yaml-Datei in den Hauptverteilungsmustern:
 ** Bereitstellung für Scanner-Pods hinzugefügt (2 Standard)
 ** Die Bereitstellung von Scanner-Pods hat einen auskommentierten Abschnitt für lokale Scanvorgänge
 ** Cron-Job für Updater-Pod zur Aktualisierung der cve-Datenbank von Scannern hinzugefügt

==== Aufrüstung von 3.x auf 4.0

Aktualisieren Sie bei Helm-Einsätzen das Helm-Diagramm auf die neue Version 1.6.0. Dann ist nur ein Standard-Upgrade auf 4.0.0 erforderlich (z. B. helm upgrade my-release --set imagePullSecrets=regsecret-neuvector,tag=4.0.0 ./neuvector-helm/).

[TIP]
====
Kubernetes (für OpenShift verwenden Sie die entsprechenden oc-Befehle)
====

* Sichern Sie die Konfiguration unter Einstellungen -> Konfiguration
* Erstellen Sie die beiden neuen Bindungen
 ** kubectl create clusterrolebinding neuvector-binding-view --clusterrole=view --serviceaccount=neuvector:default
 ** kubectl create rolebinding neuvector-admin --clusterrole=admin --serviceaccount=neuvector:default -n neuvector
* Setzen Sie die Versions-Tags für die Controller-, Manager- und Enforcer-Yaml-Dateien auf 4.0.0 und wenden Sie das Update an.
* Erstellen Sie die xref:scanners.adoc#_manual_deployment_of_multiple_scanners_on_kubernetes[Scanner-Pods]
* Erstellen oder Aktualisieren des xref:scanners.adoc#_manual_deployment_of_multiple_scanners_on_kubernetes[Scanner-Cron-Jobs]
* Warten Sie ein paar Minuten, bis die rollierende Aktualisierung der Steuerungen abgeschlossen ist, und überprüfen Sie nach dem Login alle Einstellungen...
